#guard ccs
scope ccs init on
{
	pb void uto()
	{
		if isordit
		{
			tmit=uits(tru,o2slot)
			if itc(ordit)==itc(tmit) && itch(tmit)!=0 && ordit!=tmit
			{
				SetItemCharges(tmit,itch(ordit)+itch(tmit))
				rit(ordit)
			}
		}
	}

	pr void init()
	{
		tra(uto)
	}
}